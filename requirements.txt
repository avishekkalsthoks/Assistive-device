# Core AI and Vision (Lightweight version)
opencv-python-headless>=4.8.0.0
Pillow>=10.0.0

# Audio and Voice
gTTS>=2.5.0
SpeechRecognition>=3.10.0
PyAudio>=0.2.13

# HTTP + env (Used for API calls)
requests>=2.31.0
python-dotenv>=1.0.0
